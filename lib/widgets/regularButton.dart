import 'package:flutter/material.dart';

class RegularButton extends StatelessWidget {
  const RegularButton({required String title, required Function onPressed});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
